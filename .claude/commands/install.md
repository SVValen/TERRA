# /install

Lee `.review/commands/install.md` y ejecutá sus instrucciones exactamente.

Los agentes están en `.review/agents/`.
Los checklists están en `.review/checklists/`.
El contexto del proyecto está en `AGENTS.md`.
